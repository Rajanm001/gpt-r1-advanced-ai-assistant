# ================================
# Production Environment Configuration
# ================================

# Database Configuration
POSTGRES_DB=gpt_r1_production
POSTGRES_USER=gpt_r1_user
POSTGRES_PASSWORD=change-this-secure-password-in-production

# OpenAI Configuration (REQUIRED - Add your actual API key)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Security Configuration (REQUIRED - Generate secure keys)
SECRET_KEY=your-super-secure-secret-key-minimum-32-characters-long
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Application Configuration
ENVIRONMENT=production
DEBUG=false
LOG_LEVEL=INFO

# CORS Configuration (Update with your production domains)
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com,https://www.yourdomain.com

# Frontend Configuration
NEXT_PUBLIC_API_URL=https://api.yourdomain.com
NEXT_PUBLIC_APP_NAME=GPT.R1 Professional AI Assistant

# ================================
# Security Notes
# ================================
# 1. Change all default passwords before production deployment
# 2. Generate secure random values for SECRET_KEY (minimum 32 characters)
# 3. Add your actual OpenAI API key
# 4. Update ALLOWED_ORIGINS with your production domain(s)
# 5. Update NEXT_PUBLIC_API_URL with your production API URL
# 6. Never commit this file with real secrets to version control

# ================================
# Generate Secure Values
# ================================
# Generate SECRET_KEY: openssl rand -base64 32
# Generate POSTGRES_PASSWORD: openssl rand -base64 16